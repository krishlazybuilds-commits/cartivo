export const metadata = {
  title: "Create Account — Cartivo",
  description: "Create a free Cartivo account to start shopping.",
};

export default function RegisterLayout({ children }) {
  return children;
}
