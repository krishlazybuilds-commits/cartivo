export const metadata = {
  title: "Profile — Cartivo",
  description: "Manage your Cartivo account and personal details.",
};

export default function ProfileLayout({ children }) {
  return children;
}
