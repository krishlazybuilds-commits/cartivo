export const metadata = {
  title: "Contact — Cartivo",
  description: "Get in touch with the Cartivo team.",
  alternates: {
    canonical: "/contact",
  },
};

export default function ContactLayout({ children }) {
  return children;
}
