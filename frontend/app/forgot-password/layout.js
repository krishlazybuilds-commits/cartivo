export const metadata = {
  title: "Forgot Password — Cartivo",
  description: "Reset your Cartivo account password.",
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
