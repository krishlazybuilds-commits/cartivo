export const metadata = {
  title: "Checkout — Cartivo",
  description: "Complete your purchase with secure checkout.",
  alternates: {
    canonical: "/checkout",
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
