export const metadata = {
  title: "Reset Password — Cartivo",
  description: "Set a new password for your Cartivo account.",
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
