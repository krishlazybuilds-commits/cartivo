export const metadata = {
  title: "Wishlist — Cartivo",
  description: "Products you've saved for later on Cartivo.",
  alternates: {
    canonical: "/wishlist",
  },
};

export default function WishlistLayout({ children }) {
  return children;
}
